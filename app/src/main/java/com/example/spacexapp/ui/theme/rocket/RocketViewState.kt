package com.example.spacexapp.ui.theme.rocket

import com.example.spacexapp.ui.theme.rocket.model.RocketModel

data class RocketViewState (
    val rocketList: List<RocketModel> = emptyList()
        )